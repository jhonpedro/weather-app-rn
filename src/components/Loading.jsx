import React from 'react'
import { ActivityIndicator } from 'react-native'
import colors from '../colors/constants'

const Loading = () => <ActivityIndicator color={colors.PRIMARY} size='large' />

export default Loading
